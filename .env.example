# Database (MySQL)
DB_SERVER=localhost
DB_USERNAME=root
# Use double quotes if the password contains # or other special characters
DB_PASSWORD=
DB_NAME=skriptoria_db

# Stripe (get keys from https://dashboard.stripe.com/test/apikeys)
STRIPE_PUBLISHABLE_KEY=pk_test_
STRIPE_SECRET_KEY=sk_test_
STRIPE_CURRENCY=mxn
